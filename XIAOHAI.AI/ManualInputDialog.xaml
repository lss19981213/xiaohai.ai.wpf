<Window x:Class="XIAOHAI.AI.ManualInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XIAOHAI.AI"
        Title="添加知识条目"
        Height="400" Width="600"
        ResizeMode="CanResize"
        Background="#0a0b0f"
        WindowStartupLocation="CenterOwner"
        Icon="/ziyuan/bitbug_favicon.ico">
    <Window.Resources>
        <SolidColorBrush x:Key="BgDark" Color="#0a0b0f"/>
        <SolidColorBrush x:Key="BgCard" Color="#12141c"/>
        <SolidColorBrush x:Key="Border" Color="#6366f1"/>
        <SolidColorBrush x:Key="Text" Color="#e8eaf0"/>
        <SolidColorBrush x:Key="TextMuted" Color="#8b8fa8"/>
        <SolidColorBrush x:Key="Accent" Color="#6366f1"/>
        <SolidColorBrush x:Key="AccentPurple" Color="#8b5cf6"/>
        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#6366f1" Offset="0"/>
            <GradientStop Color="#8b5cf6" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="标题：" Foreground="{StaticResource Text}" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="1" Height="30" Margin="0,0,0,15"
                 Background="{StaticResource BgCard}" Foreground="{StaticResource Text}"
                 BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="8"/>
                 
        <TextBlock Grid.Row="2" Text="内容：" Foreground="{StaticResource Text}" FontSize="14" Margin="0,0,0,5"/>
        <TextBox x:Name="ContentTextBox" Grid.Row="2" Grid.RowSpan="1" Margin="0,25,0,15"
                 Background="{StaticResource BgCard}" Foreground="{StaticResource Text}"
                 BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="8"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
                 
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Content="确定" Width="80" Height="35" Margin="0,0,10,0"
                    Background="{StaticResource AccentGradient}" Foreground="{StaticResource Text}"
                    BorderThickness="0" Cursor="Hand" Click="OkButton_Click"/>
            <Button x:Name="CancelButton" Content="取消" Width="80" Height="35"
                    Background="Transparent" Foreground="{StaticResource Text}"
                    BorderBrush="{StaticResource Border}" BorderThickness="1" Cursor="Hand" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>