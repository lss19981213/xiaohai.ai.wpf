<Window x:Class="XIAOHAI.AI.FloatingBall"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:gif="clr-namespace:WpfAnimatedGif;assembly=WpfAnimatedGif"
        Title="小海悬浮球"
        Width="120" Height="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove"
        MouseDoubleClick="Window_MouseDoubleClick"
        MouseEnter="Window_MouseEnter">
    
    <Window.Effect>
        <DropShadowEffect Color="#6366f1" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
    </Window.Effect>
    
    <Grid>
        <Border Width="100" Height="100" HorizontalAlignment="Center" VerticalAlignment="Center" CornerRadius="50" ClipToBounds="True">
            <Image gif:ImageBehavior.AnimatedSource="/ziyuan/think.gif" Stretch="Uniform"/>
        </Border>
        
        <!-- 气泡信息框 -->
        <Border x:Name="InfoBubble" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top"
                Margin="0,8,8,0"
                Background="#1e1f29"
                BorderBrush="#6366f1"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8,6"
                MaxWidth="140"
                MinWidth="100"
                Opacity="0"
                Visibility="Collapsed">
            <Border.Effect>
                <DropShadowEffect Color="#6366f1" BlurRadius="10" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            <TextBlock x:Name="InfoText" 
                       Text="小海在这里哦！" 
                       Foreground="#e8eaf0" 
                       FontSize="11"
                       TextWrapping="Wrap"
                       LineHeight="14"/>
        </Border>
    </Grid>
</Window>
